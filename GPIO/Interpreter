import ButtonsInitializer as BI
import WordBank as WB
from sys import argv
import RPi.GPIO as GPIO
letter = ""
isButton = False
didStart = False
wordlis = [WB.advlist, WB.nounlist, WB.adjlist, WB.sublist, WB.verblist, WB.alphabet, WB.saylist]
def getSentence(str sent):        //
  sentence = []
  for i in range(len(wordlis)):
    for word in wordlis[i]:
      if(sent.find(word) != -1):
        if(i == len(wordlis) - 2):
          return [word]
        elif(i == len(wordlis) - 1):
          
        else:
          sentence.append(word + " ")
          
  

    
    
        
